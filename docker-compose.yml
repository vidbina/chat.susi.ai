version: '3'

services:
   front: &client
     build: .
     ports:
     - "3333:5000"
     - "3336:3000"
     volumes:
       - ".:/usr/src"
   build:
     <<: *client
     entrypoint: npm run build
   front-dev:
     <<: *client
     entrypoint: npm run start
